# Example secrets file for Ansible (unencrypted)
# Copy to secrets and encrypt with ansible-vault:
#   cp secrets.example secrets
#   ansible-vault encrypt secrets
# Then set your pre-shared key:

ipsec_secret: "CHANGEME-StrongSharedSecret"
